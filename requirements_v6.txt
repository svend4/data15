# ============================================================
# Hybrid Orchestrator v6.0 — Dependencies
# ============================================================
#
# Установка (минимальная, только stdlib-зависимости):
#   pip install -r requirements_v6.txt
#
# Установка (полная, все возможности):
#   pip install -r requirements_v6.txt -r requirements_v6_optional.txt
#
# ============================================================

# ── Core ────────────────────────────────────────────────────
# v6 uses stdlib http.server — Flask and Werkzeug are NOT used and were
# removed to avoid pulling in ~12 MB of unused dependencies.

# ── v6: Векторная база знаний ───────────────────────────────
# Вариант A: ChromaDB (рекомендуется, production-quality)
chromadb>=0.4.0
sentence-transformers>=2.2.0   # модели для эмбеддингов (all-MiniLM-L6-v2)

# Вариант B: TF-IDF fallback встроен в stdlib — доп. пакеты не нужны
# (автоматически активируется если chromadb не установлен)

# ── v6: WebUI ──────────────────────────────────────────────
streamlit>=1.32.0

# ── v6: LLM-провайдеры ─────────────────────────────────────
# Anthropic Claude (рекомендуется)
anthropic>=0.25.0              # optional: можно использовать через urllib без пакета
# OpenAI GPT-4o
openai>=1.0.0                  # optional: аналогично
# Ollama — только localhost REST API, пакет не нужен
# MiniMax — только HTTP, пакет не нужен

# ── v6: JWT Auth ───────────────────────────────────────────
# Реализовано на stdlib (hmac + hashlib) — сторонние пакеты не нужны
# PyJWT>=2.8.0                  # опционально для совместимости со сторонними клиентами

# ── Разработка и тестирование ──────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
mypy>=1.0.0
ruff>=0.1.0

# ── Мониторинг (опционально) ───────────────────────────────
# prometheus-client>=0.17.0

# ── Масштабирование (опционально) ──────────────────────────
# redis>=4.5.0          # для distributed lock и кэширования между экземплярами
# psycopg2-binary>=2.9.0  # PostgreSQL вместо SQLite для production
# celery>=5.3.0         # распределённая очередь задач

# ── Примечание по SQLite ────────────────────────────────────
# SQLite входит в Python stdlib — дополнительная установка не нужна.
# WAL mode + thread-local connections настроены в storage/sqlite_board.py.
